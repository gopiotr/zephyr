configurations:
  db_hash_read_eatt:
    devices:
      server:
        bsim_extra_args:
          - "-testid=gatt_server_eatt"
      client:
        bsim_extra_args:
          - "-testid=gatt_client_db_hash_read_eatt"
      bsim_phy:
        application: "${BSIM_OUT_PATH}/bin/bs_2G4_phy_v1"
        bsim_extra_args:
          - "-sim_length=60e6"

  db_hash_read_no_eatt:
    devices:
      server:
        bsim_extra_args:
          - "-testid=gatt_server_no_eatt"
      client:
        bsim_extra_args:
          - "-testid=gatt_client_db_hash_read_no_eatt"
      bsim_phy:
        application: "${BSIM_OUT_PATH}/bin/bs_2G4_phy_v1"
        bsim_extra_args:
          - "-sim_length=60e6"

  out_of_sync_eatt:
    devices:
      server:
        bsim_extra_args:
          - "-testid=gatt_server_eatt"
      client:
        bsim_extra_args:
          - "-testid=gatt_client_out_of_sync_eatt"
      bsim_phy:
        application: "${BSIM_OUT_PATH}/bin/bs_2G4_phy_v1"
        bsim_extra_args:
          - "-sim_length=60e6"

  out_of_sync_no_eatt:
    devices:
      server:
        bsim_extra_args:
          - "-testid=gatt_server_no_eatt"
      client:
        bsim_extra_args:
          - "-testid=gatt_client_out_of_sync_no_eatt"
      bsim_phy:
        application: "${BSIM_OUT_PATH}/bin/bs_2G4_phy_v1"
        bsim_extra_args:
          - "-sim_length=60e6"

  retry_reads_eatt:
    devices:
      server:
        bsim_extra_args:
          - "-testid=gatt_server_eatt"
      client:
        bsim_extra_args:
          - "-testid=gatt_client_retry_reads_eatt"
      bsim_phy:
        application: "${BSIM_OUT_PATH}/bin/bs_2G4_phy_v1"
        bsim_extra_args:
          - "-sim_length=60e6"

  retry_reads_no_eatt:
    devices:
      server:
        bsim_extra_args:
          - "-testid=gatt_server_no_eatt"
      client:
        bsim_extra_args:
          - "-testid=gatt_client_retry_reads_no_eatt"
      bsim_phy:
        application: "${BSIM_OUT_PATH}/bin/bs_2G4_phy_v1"
        bsim_extra_args:
          - "-sim_length=60e6"
