common:
  devices:
    - name: "peripheral"
      bsim_extra_args:
        - "-rs=23"
    - name: "central"
      bsim_extra_args:
        - "-rs=6"
  phy:
    application: "${BSIM_OUT_PATH}/bin/bs_2G4_phy_v1"
    bsim_extra_args:
      - "-sim_length=20e6"

configurations:
  app_split:
    devices:
      - name: "peripheral"
        application: "tests/bluetooth/bsim_bt/bsim_test_gatt_caching/bluetooth.bsim.app_split_build"
        bsim_extra_args:
          - "-testid=peripheral"
          - "-RealEncryption=0"
      - name: "central"
        application: "tests/bluetooth/bsim_bt/bsim_test_gatt_caching/bluetooth.bsim.app_split_build"
        bsim_extra_args:
          - "-testid=central"
          - "-RealEncryption=0"

  app_split_encrypted:
    devices:
      - name: "peripheral"
        application: "tests/bluetooth/bsim_bt/bsim_test_gatt_caching/bluetooth.bsim.app_split_build"
        bsim_extra_args:
          - "-testid=peripheral"
          - "-RealEncryption=1"
      - name: "central"
        application: "tests/bluetooth/bsim_bt/bsim_test_gatt_caching/bluetooth.bsim.app_split_build"
        bsim_extra_args:
          - "-testid=central_encrypted"
          - "-RealEncryption=1"

  app_split_low_lat:
    devices:
      - name: "peripheral"
        application: "tests/bluetooth/bsim_bt/bsim_test_gatt_caching/bluetooth.bsim.app_split_low_lat_build"
        bsim_extra_args:
          - "-testid=peripheral"
          - "-RealEncryption=0"
      - name: "central"
        application: "tests/bluetooth/bsim_bt/bsim_test_gatt_caching/bluetooth.bsim.app_split_low_lat_build"
        bsim_extra_args:
          - "-testid=central"
          - "-RealEncryption=0"

  app_split_privacy_encrypted:
    devices:
      - name: "peripheral"
        application: "tests/bluetooth/bsim_bt/bsim_test_gatt_caching/bluetooth.bsim.app_split_privacy_encrypted_build"
        bsim_extra_args:
          - "-testid=peripheral"
          - "-RealEncryption=1"
      - name: "central_encrypted"
        application: "tests/bluetooth/bsim_bt/bsim_test_gatt_caching/bluetooth.bsim.app_split_privacy_encrypted_build"
        bsim_extra_args:
          - "-testid=central_encrypted"
          - "-RealEncryption=1"
