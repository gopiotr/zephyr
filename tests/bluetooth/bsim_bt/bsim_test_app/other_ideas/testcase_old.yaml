common:
  build_only: true
  extra_args:
    - "-DCMAKE_C_FLAGS='-Werror'"
    - "-DCONFIG_COVERAGE=y"
    - "-DCMAKE_EXPORT_COMPILE_COMMANDS=ON"

tests:
  bluetooth.bsim.app_split_build:
    extra_args:
      - "-DCONF_FILE=prj_split.conf"

  bluetooth.bsim.app_split_low_lat_build:
    extra_args:
      - "-DCONF_FILE=prj_split_low_lat.conf"

  bluetooth.bsim.app_split_privacy_encrypted_build:
    extra_args:
      - "-DCONF_FILE=prj_split_privacy.conf"
